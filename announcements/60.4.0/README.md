# Discord announcement — 60.4.0

Changes since v59.0.0 through release commit 722b6be (60.4.0).

Publish as two messages in the same order as the previous announcement:

1. `discord-en.md` with `update-en.png`.
2. `discord-ru.md` with `update-ru.png`.

Each message is under 2,000 characters. Only the English message includes `@everyone` to avoid a second ping. `discord.md` is the combined editorial copy, not a single Discord message.

Both PNG banners are 1672 × 941, approximately 16:9. Created with built-in image_gen; prompts are saved in `image-prompt.md`. The illustrated vote grid and rewards are symbolic artwork, not interface screenshots.

Verified topics: lobby redesign (60.4.0), vote table and profile redesign (59.1.0), achievement redesign (59.2.0), language/SEO/loading changes (60.0.0–60.3.0). Stickers themselves shipped in 59.0.0 and are not announced as a new feature again. No measured page-speed improvement is claimed.

Prepared locally; not posted to Discord.
