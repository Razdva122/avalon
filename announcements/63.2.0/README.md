# Discord announcement — 63.2.0

Changes after `v60.4.0` through `v63.2.0` (`feb9084`). Follows the text and illustrated-banner format in `announcements/60.4.0`.

Prepared locally; not posted to Discord.

Publish as two separate messages:

1. `discord-en.md` with `update-en-v2.png`.
2. `discord-ru.md` with `update-ru-v2.png`.

Each message is under 2,000 characters. Only the English message includes `@everyone`, to avoid a second ping. `discord.md` is the combined editorial copy, not a single Discord message.

## Sources checked

- Support and Premium: final support-page translations, direct-transfer flow and backend Premium eligibility. Lifetime Premium requires $10 total confirmed support; a monthly TrueSkill reset replaces the standard three-month cooldown. Existing game history and achievements remain.
- Cosmetics: `premiumCosmetics.ts`; exactly two Premium avatars and two Premium stickers, introduced in 61.2.0.
- Community and acknowledgements: community directory and About page changes, including 63.2.0. The directory currently contains the official server; copy does not claim additional servers already exist.
- Password recovery: 62.0.0 and recovery documentation; recovery uses the email saved on the account.
- Game settings: 61.6.0, grouped controls, help, related-role validation and undo.
- Loading and fixes: image optimization, 61.7.0 player-statistics loading, preview statistics and role-translation fixes. No numerical performance claims.

This is a release-code review, not a production deployment check. Recovery email and donation availability depend on server configuration; live delivery and payment processing were not exercised.

## Images

Created with the built-in image_gen tool. Prompts are saved in `image-prompt.md`. The final v2 banners use the user-provided 59.0.0 announcement as their style reference and the four actual Premium cosmetic assets as character references. The Russian version localizes the English banner. These are AI-generated promotional illustrations, not screenshots or pixel-exact copies of the assets. The earlier update-en.png is an unused draft; publish the v2 files listed above.
