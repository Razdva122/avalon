# Copy to /etc/avalon-voice-watchdog.env on the media VM.
# Use the backend VM's private VPC IP or private DNS, never the public hostname.
VOICE_HEALTH_URL=http://BACKEND_PRIVATE_IP:7882/health/voice
VOICE_COMPOSE_FILE=/opt/avalon/deploy/voice/compose.yaml
