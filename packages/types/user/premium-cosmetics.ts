/** Controls premium cosmetics, community directory and home banners. Support always previews the full collection. */
export const PREMIUM_COSMETICS_ENABLED: boolean = process.env.NODE_ENV === 'development';
